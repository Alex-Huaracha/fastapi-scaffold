# The +asyncpg driver is required. Without it SQLAlchemy falls back to its
# default PostgreSQL driver (psycopg2), which is synchronous and not installed.
DATABASE_URL=postgresql+asyncpg://user:password@localhost/postgres_db
